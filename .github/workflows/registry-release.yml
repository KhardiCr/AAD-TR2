# Auto-generated by Arcana provisioning
name: registry-release
on:
  push:
    tags: ['v*']

jobs:
  registry-release:
    uses: KhardiCr/github-workflows/.github/workflows/registry-release.yml@main
    with:
      image-name: "myproject/payment-service"
    secrets: inherit
